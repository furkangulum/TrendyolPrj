@model IEnumerable<TrendyolMVC2016.Models.Order>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<p>
    @Html.ActionLink("Accepted Orders", "AcceptedOrders")
</p>
<h3>Waiting Orders To Accept to the Warehouse</h3>

@*<table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Order.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Status)
                </th>
                <th></th>
            </tr>

        @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Order.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Status)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                        @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                    </td>
                </tr>
            }

    </table>*@
@using (Html.BeginForm("WaitingProducts","Storages"))
{
    @Html.AntiForgeryToken()
    <div class="form-group">
        @foreach (var i in Model)
        {

        <input type="checkbox" name="WaitingProducts" value="@i.Id"><span>Product Name : @i.Product.ProductName, Product Code : @i.Product.ProductCode, Product Piece : @i.Piece  <br /></span>
        }

    </div>

    <div class="form-actions no-color">
        <input type="submit" value="Accept Selected Orders To Warehouse" class="btn btn-default" /> 
    </div>
}

